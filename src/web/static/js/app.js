// Sports Predictor — minimal client JS.
// Most interactivity is server-driven via HTMX (loaded in base.html).
// This file is for any small enhancements that don't fit cleanly into HTMX.

document.addEventListener("DOMContentLoaded", () => {
  // Placeholder. Add tooltip toggles, keyboard shortcuts, etc. here later.
});
